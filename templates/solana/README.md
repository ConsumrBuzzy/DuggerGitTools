# {{PROJECT_NAME}}

{{PROJECT_NAME}} is a new Solana project initialized with DuggerCore-Universal.

## 🚀 Quick Start

This project comes with DuggerCore-Universal pre-configured for optimal development workflow.

### Prerequisites

- Rust 1.70+ installed
- Python 3.11+ installed
- Solana CLI installed
- Anchor CLI installed
- DuggerCore-Universal CLI

### Setup

```bash
# Install Python dependencies
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -e .

# Build Anchor programs
anchor build

# Run tests
anchor test
```

### Development

```bash
# Build Rust programs
cargo build-bpf

# Format code (handled automatically by DGT)
cargo fmt
black .
ruff check --fix .

# Lint code (handled automatically by DGT)
cargo clippy
npx eslint --fix .

# Commit with DuggerCore
dgt commit
```

## 📁 Project Structure

```
{{PROJECT_NAME}}/
├── programs/      # Anchor programs (Rust)
├── tests/         # Anchor tests
├── client/        # Python client code
├── scripts/       # Utility scripts
├── Anchor.toml    # Anchor configuration
├── Cargo.toml     # Rust workspace configuration
├── pyproject.toml # Python configuration
├── dugger.yaml    # DuggerCore configuration
└── README.md      # This file
```

## ⚙️ DuggerCore Configuration

The `dugger.yaml` file is pre-configured with:

- **Multi-Provider**: Rust and Python tool orchestration
- **Auto-formatting**: `cargo fmt`, `black`, `ruff` on commit
- **Auto-linting**: `cargo clippy`, `eslint` with fixes
- **Version management**: Synchronized version bumping
- **Smart commit messages**: Solana-aware categorization

## 🎯 DuggerCore Features

- **Multi-Provider Orchestration**: Coordinates Rust and Python workflows
- **Capability Detection**: Automatically detects available tools
- **Universal Rollback**: Safe operations with automatic rollback
- **Performance Caching**: Cached tool detection for speed
- **Solana-Aware**: Specialized handling for Solana projects

## 📝 Development Workflow

1. Make your changes (Rust programs or Python client)
2. Run `dgt commit` - DGT will:
   - Auto-format all code (Rust + Python)
   - Run linting and fixes for all languages
   - Generate smart commit message with Solana context
   - Handle version management across all files
   - Commit and push if configured

## 🔧 Customization

Edit `dugger.yaml` to customize:
- Multi-provider execution order
- Tool configurations per provider
- Commit message style
- Version bumping strategy
- Build commands

## 📚 Documentation

- [DuggerCore Documentation](../../README.md)
- [Solana Documentation](https://docs.solana.com/)
- [Anchor Documentation](https://www.anchor-lang.com/)
- [Rust Book](https://doc.rust-lang.org/book/)

---

*Generated by DuggerCore-Universal on {{TIMESTAMP}}*
